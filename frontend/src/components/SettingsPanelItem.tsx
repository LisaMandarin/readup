import { CollapsiblePanelItem } from './CollapsiblePanel'

export default function SettingsPanelItem() {
  return (
    <CollapsiblePanelItem
      title="Settings"
      description="Configure app behavior and preferences once this area is ready."
    />
  )
}
